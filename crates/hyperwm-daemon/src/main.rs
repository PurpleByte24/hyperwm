//! Scaffold only. Event Router + Action Engine wiring lands starting in
//! build unit 5 (see CLAUDE.md build order).

fn main() {
    println!("hyperwm-daemon: not yet implemented (see CLAUDE.md build order)");
}
