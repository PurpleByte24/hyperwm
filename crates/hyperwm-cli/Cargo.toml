[package]
name = "hyperwm-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "hyperwm CLI: talks to the running daemon over a Unix domain socket (reload, status, verify) plus install-keymap. Implemented starting in build unit 7."

[dependencies]
hyperwm-config = { path = "../hyperwm-config" }

[[bin]]
name = "hyperwm"
path = "src/main.rs"
